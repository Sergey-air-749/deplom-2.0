import { generateComponents } from "@uploadthing/react";
 
// Используем generateComponents для создания хуков
// Поскольку у вас чистый JS, мы просто вызываем функцию
export const { useUploadThing } = generateComponents({});